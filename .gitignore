# --- Dependencies ---
node_modules/
package-lock.json
# Nếu bạn dùng yarn thì bỏ comment dòng dưới
# yarn.lock

# --- Environment Variables (QUAN TRỌNG: Bảo mật) ---
.env
.env.*
! .env.example

# --- Build Outputs ---
dist/
build/
.vite/
web-build/

# --- Mobile (React Native / Expo) ---
.expo/
.expo-shared/
ios/
android/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.apk
*.aab

# --- Logs ---
logs
*.log
npm-debug.log*
yarn-debug.log*

# --- OS & IDE Files ---
.DS_Store
.vscode/
.idea/
Thumbs.db

# --- DATABASE & UPLOADS (QUAN TRỌNG CHO DOCIFY) ---
# Chặn file DB cục bộ (nếu có)
*.db
*.sqlite

# Chặn file đã upload vào Server
server/uploads/*
# Giữ lại file .gitkeep để Git biết là cần tạo thư mục uploads
!server/uploads/.gitkeep